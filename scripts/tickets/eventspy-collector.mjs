#!/usr/bin/env node
import {readFile,rename,writeFile} from "node:fs/promises";import{randomUUID}from"node:crypto";import{resolve}from"node:path";
import{EVENTSPY_COVERAGE,eventSpyCoverageForGame,validateEventSpyCoverage}from"../../src/lib/tickets/eventspy-coverage.mjs";import{buildMarketObservation,parseEventSpyTooltipText}from"../../src/lib/tickets/market-observation.mjs";import{eventSpyGameRoot,eventSpyPacificDate,readEventSpyLedger,reserveEventSpyAttempt}from"./eventspy-ledger.mjs";import{eventSpyHistoryCurrent,publishEventSpyHistory,readEventSpyHistoryForGame}from"./eventspy-history.mjs";
const code=e=>/^[A-Z][A-Z0-9_]{1,63}$/.test(e?.code)?e.code:"EVENTSPY_COLLECTION_FAILED",error=(m,c,diagnostic)=>Object.assign(new Error(m),{code:c,diagnostic}),baseCounts=()=>({manifestRows:17,authorized:16,unavailable:1,attempted:0,succeeded:0,failed:0,dailyLimited:0,skipped:1});
const DATE=/(?:\b(?:Mon|Tue|Wed|Thu|Fri|Sat|Sun)(?:day)?\b[,]?\s+)?\b(?:Jan(?:uary)?|Feb(?:ruary)?|Mar(?:ch)?|Apr(?:il)?|May|Jun(?:e)?|Jul(?:y)?|Aug(?:ust)?|Sep(?:t(?:ember)?)?|Oct(?:ober)?|Nov(?:ember)?|Dec(?:ember)?)\.?\s+\d{1,2}(?:st|nd|rd|th)?[,]?\s+\d{4}\b/i,ACCESS=/access denied|forbidden|unauthorized|verify (?:you are|that you are) human|captcha|bot challenge|security check|checking your browser|just a moment|too many requests|rate limit|javascript (?:is )?required|enable javascript|service unavailable|temporarily unavailable/i;
const bounded=(v,n=160)=>typeof v==="string"&&v.length<=n&&/^[\x20-\x7e]+$/.test(v)?v:undefined,texts=async l=>{try{return(await l.allInnerTexts()).map(x=>String(x).trim()).filter(Boolean).slice(0,20)}catch{return[]}};
export function normalizeEventSpyMatchup(v){return String(v).normalize("NFKC").toLowerCase().replace(/[\u00a0\s]+/g," ").replace(/\b(?:versus|vs\.?|v\.?)\b/g," vs ").replace(/[^a-z0-9]+/g," ").trim()}
export const expectedEventSpyMatchup=m=>normalizeEventSpyMatchup(m.homeAway==="home"?`Seattle Seahawks vs ${m.opponent}`:`${m.opponent} vs Seattle Seahawks`);
export function normalizeEventSpyDate(v){const hit=String(v).normalize("NFKC").match(DATE);if(!hit)return null;const p=hit[0].replace(/^(?:Mon|Tue|Wed|Thu|Fri|Sat|Sun)(?:day)?[,]?\s+/i,"").replace(/(\d)(?:st|nd|rd|th)/i,"$1").replace(/[,.]/g,"").trim().match(/^([A-Za-z]+)\s+(\d{1,2})\s+(\d{4})$/);if(!p)return null;const ms={jan:1,january:1,feb:2,february:2,mar:3,march:3,apr:4,april:4,may:5,jun:6,june:6,jul:7,july:7,aug:8,august:8,sep:9,sept:9,september:9,oct:10,october:10,nov:11,november:11,dec:12,december:12},m=ms[p[1].toLowerCase()];return m?`${p[3]}-${String(m).padStart(2,"0")}-${String(+p[2]).padStart(2,"0")}`:null}
export function validateUserAgent(v){if(typeof v!=="string"||!v.length||v.length>512||/[\u0000-\u001f\u007f]/.test(v))throw error("Invalid user agent.","EVENTSPY_CONFIG_INVALID");return v}
const configOk=c=>{validateEventSpyCoverage(c.coverage);validateUserAgent(c.userAgent);if(c.maxAttempts!==2||c.navigationsPerGame!==1)throw error("Unsafe limits.","EVENTSPY_CONFIG_INVALID");if("EVENTSPY_SOURCE_URL"in process.env||"EVENTSPY_GAME_ID"in process.env)throw error("Mapping overrides prohibited.","EVENTSPY_CONFIG_INVALID")};
async function record(root,m,outcome,now){const parent=eventSpyGameRoot(root,m.gameId),temp=`${parent}/.collection-state-${randomUUID()}.tmp`,v={schemaVersion:1,gameId:m.gameId,sourceEventId:m.sourceEventId,recordedAt:new Date(now).toISOString(),outcome};await writeFile(temp,`${JSON.stringify(v)}\n`,{flag:"wx",mode:0o600});await rename(temp,`${parent}/collection-state.json`)}const safeRecord=(...a)=>record(...a).catch(()=>{});
function urlFacts(v,m){try{const u=new URL(v),p=u.pathname.split("/").filter(Boolean),allowlisted=u.protocol==="https:"&&u.hostname==="www.event-spy.com"&&!u.username&&!u.password&&!u.port&&!u.search&&!u.hash;return{allowlisted,exactUrlMatch:u.href===m.sourceUrl,sourceEventIdPathMatch:p.length===3&&p[0]==="event"&&p[2]===m.sourceEventId}}catch{return{allowlisted:false,exactUrlMatch:false,sourceEventIdPathMatch:false}}}
async function classify(page){const h=await texts(page.getByRole("heading")),d=await texts(page.getByRole("dialog")),title=await page.title().catch(()=>"");const s=[title,...h,...d];return{headings:h,blocked:s.some(x=>ACCESS.test(x)),kind:s.some(x=>/captcha|human|bot|security check|checking your browser/i.test(x))?"challenge":s.some(x=>/rate limit|too many/i.test(x))?"rate_limit":s.some(x=>/javascript/i.test(x))?"javascript_required":s.some(x=>ACCESS.test(x))?"access_or_error":"none"}}
const fail=(c,d)=>{d.outcome=c;throw error(c,c,d)};
export async function extractLatestTooltip(page,m,config={}){const d={gameId:m.gameId,requestedUrl:m.sourceUrl,mainDocumentHttpStatus:null,exactUrlMatch:false,sourceEventIdPathMatch:false,plausibleEventHeadingCount:0,matchupMatch:false,dateFound:false,dateMatch:false,chartHeadingFound:false,chartApplicationFound:false,consentDialogClassification:"none",accessClassification:"none",outcome:"EVENTSPY_COLLECTION_FAILED"};let escaped=false;page.on("popup",x=>{x.close().catch(()=>{});escaped=true});page.on("download",x=>{x.cancel().catch(()=>{});escaped=true});page.on("framenavigated",f=>{if(f===page.mainFrame()&&f.url()!=="about:blank"&&f.url()!==m.sourceUrl)escaped=true});const response=await page.goto(m.sourceUrl,{waitUntil:"domcontentloaded",timeout:config.navigationTimeoutMs??20000});d.mainDocumentHttpStatus=response?.status?.()??null;const facts=urlFacts(page.url(),m);Object.assign(d,facts);if(facts.allowlisted)d.finalUrl=page.url();if(Number.isInteger(d.mainDocumentHttpStatus)&&(d.mainDocumentHttpStatus<200||d.mainDocumentHttpStatus>=400)fail("EVENTSPY_HTTP_STATUS_REJECTED",d);if(!facts.exactUrlMatch||!facts.sourceEventIdPathMatch||escaped)fail("EVENTSPY_UNAPPROVED_REDIRECT",d);let c=await classify(page);d.accessClassification=c.kind;if(c.blocked)fail("EVENTSPY_ACCESS_BLOCKED",d);
 const consent=page.getByRole("dialog").filter({hasText:/consent|cookie|privacy/i});if(await consent.count()){d.consentDialogClassification="present";const b=consent.getByRole("button",{name:/^(?:accept(?: all)?|agree|continue|dismiss|close)$/i}).first();try{if(!await b.count())throw Error();await b.click({timeout:2000});await consent.first().waitFor({state:"hidden",timeout:2000});d.consentDialogClassification="dismissed"}catch{fail("EVENTSPY_CONSENT_BLOCKED",d)}}c=await classify(page);d.accessClassification=c.kind;if(c.blocked)fail("EVENTSPY_ACCESS_BLOCKED",d);
 const plausible=c.headings.filter(x=>x.length<=160&&/\b(?:vs\.?|v\.?|versus)\b/i.test(x));d.plausibleEventHeadingCount=plausible.length;if(!plausible.length)fail("EVENTSPY_HEADING_MISSING",d);if(plausible.length===1)d.detectedEventHeading=bounded(plausible[0]);d.matchupMatch=plausible.filter(x=>normalizeEventSpyMatchup(x)===expectedEventSpyMatchup(m)).length===1;if(!d.matchupMatch)fail("EVENTSPY_MATCHUP_MISMATCH",d);
 const raw=await texts(page.getByText(DATE,{exact:false})),dates=[...new Set(raw.flatMap(x=>[...x.matchAll(new RegExp(DATE.source,"ig"))].map(y=>y[0])))].slice(0,10);d.dateFound=dates.length>0;d.dateMatch=dates.some(x=>normalizeEventSpyDate(x)===m.localDate);if(dates.length===1)d.detectedEventDate=bounded(dates[0],80);if(!d.dateFound)fail("EVENTSPY_DATE_MISSING",d);if(!d.dateMatch)fail("EVENTSPY_DATE_MISMATCH",d);
 const heading=page.getByRole("heading",{name:/^Lowest Ticket Price History$/i}).first();d.chartHeadingFound=await heading.count()>0;if(!d.chartHeadingFound)fail("EVENTSPY_CHART_MISSING",d);try{await heading.waitFor({state:"visible",timeout:config.renderTimeoutMs??15000})}catch{fail("EVENTSPY_CHART_MISSING",d)}const chart=heading.locator("xpath=following::*[@role='application'][1]");d.chartApplicationFound=await chart.count()===1;if(!d.chartApplicationFound)fail("EVENTSPY_CHART_MISSING",d);try{await chart.waitFor({state:"visible",timeout:config.renderTimeoutMs??15000})}catch{fail("EVENTSPY_CHART_MISSING",d)}const wrapper=chart.locator("xpath=ancestor::*[contains(concat(' ', normalize-space(@class), ' '), ' recharts-wrapper ')][1]");if(await wrapper.count()!==1)fail("EVENTSPY_TOOLTIP_UNAVAILABLE",d);await chart.focus();let best=null,last=null,unchanged=0;for(let i=0;i<200&&unchanged<3;i++){await chart.press("ArrowRight");const tips=wrapper.locator(".recharts-tooltip-wrapper:visible");if(await tips.count()!==1)fail("EVENTSPY_TOOLTIP_UNAVAILABLE",d);let point;try{point=parseEventSpyTooltipText(await tips.first().innerText({timeout:config.parseTimeoutMs??5000}),{now:config.now??Date.now()})}catch{fail("EVENTSPY_TOOLTIP_INVALID",d)}unchanged=point.sourcePointAt===last?unchanged+1:0;last=point.sourcePointAt;if(!best||Date.parse(point.sourcePointAt)>Date.parse(best.sourcePointAt))best=point;if(page.url()!==m.sourceUrl||escaped)fail("EVENTSPY_UNAPPROVED_REDIRECT",d)}if(!best||unchanged<3)fail("EVENTSPY_TOOLTIP_UNAVAILABLE",d);d.outcome="EVENTSPY_COLLECTION_SUCCESS";return config.diagnostic?{point:best,diagnostic:d}:best}
async function open(config,override){const chromium=override??(await import("playwright-core")).chromium,browser=await chromium.launch({headless:true});try{return{browser,context:await browser.newContext({userAgent:validateUserAgent(config.userAgent),storageState:undefined,httpCredentials:undefined,acceptDownloads:false})}}catch(e){await browser.close().catch(()=>{});throw e}}
export async function browserExtractBatch(config,jobs,override){const{browser,context}=await open(config,override);try{const out=[];for(const job of jobs){let page;try{page=await context.newPage();const v=await extractLatestTooltip(page,job.mapping,{...config,now:job.now});out.push({job,point:v.point??v,diagnostic:v.diagnostic})}catch(e){out.push({job,error:code(e),diagnostic:e.diagnostic})}finally{await page?.close().catch(()=>{})}}return out}finally{await context.close().catch(()=>{});await browser.close().catch(()=>{})}}
async function fixture(jobs,fn,config){const out=[];for(const job of jobs)try{out.push({job,point:await fn(job.mapping,{...config,now:job.now})})}catch(e){out.push({job,error:code(e)==="EVENTSPY_COLLECTION_FAILED"?"EVENTSPY_TOOLTIP_INVALID":code(e)})}return out}
async function publishResult(config,result,now,publish=publishEventSpyHistory){const{mapping,reservation}=result.job;if(result.error){await safeRecord(config.root,mapping,result.error,now);return result.error}try{const collectedAt=new Date(now).toISOString(),observation=buildMarketObservation(result.point,{mapping,collectedAt,attemptId:reservation.attemptId,now});await publish(eventSpyHistoryCurrent(config.root,mapping.gameId),observation,{now,mapping});await safeRecord(config.root,mapping,"EVENTSPY_COLLECTION_SUCCESS",now);return{collectedAt,sourcePointAt:observation.sourcePointAt}}catch(e){const outcome=code(e)==="EVENTSPY_COLLECTION_FAILED"?"EVENTSPY_PUBLISH_FAILED":code(e);await safeRecord(config.root,mapping,outcome,now);return outcome}}
export async function runEventSpyCollector(config,options={}){configOk(config);const counts=baseCounts(),outcomes=[];if(!config.enabled)return{outcome:"EVENTSPY_COLLECTION_DISABLED",counts,outcomes};const now=options.now??Date.now(),jobs=[];for(const mapping of options.coverage??config.coverage){if(mapping.state!=="authorized"){outcomes.push({gameId:mapping.gameId,outcome:"EVENTSPY_SOURCE_UNAVAILABLE"});continue}let r;try{r=await reserveEventSpyAttempt(config.root,mapping.gameId,now)}catch(e){counts.failed++;outcomes.push({gameId:mapping.gameId,outcome:code(e)});continue}if(!r.allowed){counts.dailyLimited++;outcomes.push({gameId:mapping.gameId,outcome:r.outcome});continue}counts.attempted++;jobs.push({mapping,reservation:r,now})}const results=options.extractOne?await fixture(jobs,options.extractOne,config):await(options.extractBatch??browserExtractBatch)(config,jobs);for(const result of results){const value=await publishResult(config,result,now,options.publish);if(typeof value==="string"){counts.failed++;outcomes.push({gameId:result.job.mapping.gameId,outcome:value})}else{counts.succeeded++;outcomes.push({gameId:result.job.mapping.gameId,outcome:"EVENTSPY_COLLECTION_SUCCESS",...value})}}const outcome=!counts.attempted?"EVENTSPY_BATCH_NO_NAVIGATION":!counts.succeeded?"EVENTSPY_BATCH_ZERO_SUCCESS":counts.failed?"EVENTSPY_BATCH_PARTIAL_FAILURE":"EVENTSPY_BATCH_SUCCESS";return{outcome,counts,outcomes}}
export async function diagnoseEventSpyGame(config,gameId,options={}){configOk(config);if(!config.enabled)throw error("Diagnostic disabled.","EVENTSPY_COLLECTION_DISABLED");const mapping=eventSpyCoverageForGame(gameId);if(!mapping||mapping.state!=="authorized")throw error("Game not authorized.","EVENTSPY_GAME_UNAUTHORIZED");const now=options.now??Date.now(),reservation=await reserveEventSpyAttempt(config.root,mapping.gameId,now);if(!reservation.allowed)return{gameId:mapping.gameId,requestedUrl:mapping.sourceUrl,outcome:reservation.outcome};const [r]=await(options.extractBatch??browserExtractBatch)({...config,diagnostic:true},[{mapping,reservation,now}],options.chromium);const value=await publishResult(config,r,now,options.publish);if(typeof value==="string")return r.diagnostic??{gameId:mapping.gameId,requestedUrl:mapping.sourceUrl,outcome:value};return{...r.diagnostic,outcome:"EVENTSPY_COLLECTION_SUCCESS",...value}}
export async function eventSpyStatus(config,now=Date.now()){configOk(config);const today=eventSpyPacificDate(now),games=[];for(const m of config.coverage){let attempts=0,ledgerState="valid";if(m.state==="authorized")try{const l=await readEventSpyLedger(config.root,m.gameId,now);attempts=l.days.find(x=>x.date===today)?.attempts??0}catch{ledgerState="invalid"}games.push({gameId:m.gameId,state:m.state,reasonCode:m.reasonCode,attempts,ledgerState})}return{outcome:"EVENTSPY_STATUS",pacificDate:today,counts:baseCounts(),games}}
export async function validateLocal(config,now=Date.now()){configOk(config);for(const m of config.coverage.filter(x=>x.state==="authorized")){await readEventSpyLedger(config.root,m.gameId,now);try{await readEventSpyHistoryForGame(config.root,m,{now})}catch(e){if(e.code!=="ENOENT")throw e}}return{outcome:"EVENTSPY_VALIDATE_OK",counts:baseCounts()}}
export async function rehearse(config,{fixtureText,now=Date.now()}={}){if(!config.rehearsalRoot)throw error("Explicit rehearsal root required.","EVENTSPY_CONFIG_INVALID");const c={...config,root:config.rehearsalRoot,enabled:true},t=fixtureText??await readFile(new URL("./fixtures/eventspy-tooltip.txt",import.meta.url),"utf8");return runEventSpyCollector(c,{now,extractOne:async()=>parseEventSpyTooltipText(t,{now})})}
export const collectorExitCode=r=>r?.outcome==="EVENTSPY_BATCH_PARTIAL_FAILURE"?2:r?.outcome==="EVENTSPY_BATCH_ZERO_SUCCESS"?3:r?.outcome==="EVENTSPY_BATCH_NO_NAVIGATION"?4:r?.outcome==="EVENTSPY_COLLECTION_DISABLED"?1:0;
export function configFromEnv(e=process.env){return{enabled:e.EVENTSPY_ENABLED==="true",root:resolve(e.EVENTSPY_RUNTIME_ROOT||"runtime/eventspy"),rehearsalRoot:e.EVENTSPY_REHEARSAL_ROOT?resolve(e.EVENTSPY_REHEARSAL_ROOT):null,userAgent:validateUserAgent(e.EVENTSPY_USER_AGENT),coverage:EVENTSPY_COVERAGE,maxAttempts:Number(e.EVENTSPY_MAX_ATTEMPTS_PER_GAME_PER_PACIFIC_DAY??2),navigationsPerGame:Number(e.EVENTSPY_NAVIGATIONS_PER_GAME_PER_RUN??1),navigationTimeoutMs:Number(e.EVENTSPY_NAVIGATION_TIMEOUT_MS??20000),renderTimeoutMs:Number(e.EVENTSPY_RENDER_TIMEOUT_MS??15000),parseTimeoutMs:Number(e.EVENTSPY_PARSE_TIMEOUT_MS??5000)}}
const emit=(v,s=process.stdout)=>s.write(`${JSON.stringify(v)}\n`);if(process.argv[1]&&import.meta.url===new URL(`file://${process.argv[1]}`).href){try{const config=configFromEnv(),cmd=process.argv[2]??"collect";let r;if(cmd==="validate")r=await validateLocal(config);else if(cmd==="rehearse")r=await rehearse(config);else if(cmd==="status"&&process.argv.includes("--json"))r=await eventSpyStatus(config);else if(cmd==="collect")r=await runEventSpyCollector(config);else if(cmd==="diagnose"){const i=process.argv.indexOf("--game");if(i<0||i!==process.argv.length-2)throw error("Require --game.","EVENTSPY_COMMAND_INVALID");r=await diagnoseEventSpyGame(config,process.argv[i+1])}else throw error("Unknown command.","EVENTSPY_COMMAND_INVALID");emit(r);process.exitCode=collectorExitCode(r)}catch(e){emit({outcome:code(e)},process.stderr);process.exitCode=1}}
